# =========================================================
# Resume Matching Agent - Python dependencies
# Python 3.11+ recommended
# =========================================================

# --- Core LLM / Agent stack ---
langgraph>=0.2.40
langchain>=0.3.7
langchain-core>=0.3.15
langchain-community>=0.3.5
langchain-openai>=0.2.5
openai>=1.54.0

# --- Vector store + embeddings (RAG) ---
faiss-cpu>=1.8.0
sentence-transformers>=3.0.1
numpy>=1.26.0

# --- Data / typing / config ---
pydantic>=2.8.0
python-dotenv>=1.0.1
tiktoken>=0.7.0

# --- File parsing (PDF / DOCX support) ---
pypdf>=4.3.1
python-docx>=1.1.2

# --- UI ---
streamlit>=1.39.0
pandas>=2.2.2
# `tabulate` is optional for `pandas.DataFrame.to_markdown(...)`.  The app
# builds markdown tables manually so it works without it, but installing it
# unlocks `df.to_markdown()` for any future code paths.
tabulate>=0.9.0

# --- Testing ---
pytest>=8.3.0
pytest-asyncio>=0.24.0
